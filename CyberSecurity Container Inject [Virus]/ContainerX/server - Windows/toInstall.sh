#
# to remember: protected, private, public ;
#              const, let, var, static ;
#              abstract, class, interface, struct/type_with_dictionary, extends;
#

sudo apt install -y (nodejs) npm or (npm install -g nodemon)
sudo npm install -g sass nodemon typescript

npm install @types/node @types/node-sass @types/express
npm install express node-sass sass-convert ts-node

#
#
# vscode -> HTML5 Extension Pack
# vscode -> Javascript Extension Pack
# vscode -> Node Extension Pack
# vscode -> Node.js Extension Pack
# vscode -> Sass Extension Pack
# vscode -> TypeScript Extension Pack
#
#
# "scripts" :
# {
#    "sass" : "node-sass -w src/public/style/ -o dist/public/style/",
# },
# 
# "private": true,
#
#